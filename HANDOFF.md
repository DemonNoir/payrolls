# Project Handoff

## Project Overview
โปรเจกต์ปฏิทิน OT (Overtime Calendar) เป็นแอปพลิเคชันบนเว็บสำหรับให้พนักงานบันทึกเวลาทำงานล่วงเวลา (OT), ลางาน, เข้ากะดึก, และวันหยุดนักขัตฤกษ์ โดยระบบจะคำนวณเงินเดือน, สวัสดิการ (ค่าเดินทาง, ค่าอาหาร, ค่ากะดึก, ค่าอาหาร OT) ให้อัตโนมัติ รองรับการบันทึกแบบ Single-day และ Batch Edit (หลายวัน)

## Current State
- บันทึกโค้ดเวอร์ชันล่าสุดขึ้น GitHub เรียบร้อยแล้ว (`main` branch commit `8ebff1d`)
- ผ่านการทดสอบอัตโนมัติ 100% (29/29 assertions ใน `tests/calc-tests.html`)
- Cache version ล่าสุด: `sw.js` (`ot-v70`), `style.css` (`?v=155`), `ui.js` (`?v=53`)

## Recent Changes (เซสชันล่าสุด)
1. **OT Goal Calculator (ระบบวางแผน OT อัจฉริยะในแดชบอร์ด):**
   - เพิ่มการ์ด **"🎯 วางแผน OT สิ้นรอบบิล"** ในหน้าแดชบอร์ด
   - คำนวณแบบ Forward-looking จาก **วันพรุ่งนี้จนถึงวันตัดรอบบิล** เท่านั้น (ไม่คิดย้อนหลัง เพราะ OT ย้อนหลังทำไม่ได้จริง)
   - ปรับตามข้อเท็จจริงของบริษัท: **พนักงานกำหนดเรตเองไม่ได้** โดยระบบใช้เรตมาตรฐานวันทำงานปกติของบริษัทคือ **1.5 เท่า** เป็นฐานหลัก
   - รวมสวัสดิการ **ค่าอาหาร OT (50 บาท/วัน)** สำหรับกะมาตรฐาน 2 ชม. เข้าร่วมในการคำนวณอย่างถูกต้อง
   - เสนอ 2 แผนปฏิบัติจริง:
     - **แผนกะมาตรฐาน (วันละ 2 ชม.):** แสดงจำนวนวันที่ต้องทำ พร้อมเช็กว่าพอในวันทำงานปกติที่เหลือหรือไม่
     - **แผนเกลี่ยทุกวัน:** เฉลี่ยชั่วโมงต่อวันในวันทำงานที่เหลือ พร้อมประเมินระดับความหนัก (พอดี / ค่อนข้างหนัก / หนักเกินไป)
   - แจ้งเตือนความเป็นไปได้อย่างสมจริง หากวันทำงานปกติไม่พอ จะระบุให้ทราบว่าต้องรอกะ OT วันหยุดพิเศษ (3 เท่า) จากทางบริษัท
2. **Cache Busting & PWA Sync:**
   - อัปเดต `sw.js` เป็น `ot-v68`
   - อัปเดต `index.html` (CSS `v153`, `ui.js?v=51`)
   - ผ่านการทดสอบ `tests/calc-tests.html` 29/29 ครบถ้วน

## Pending Tasks / Next Steps
- ตรวจสอบฟีดแบ็กการใช้งานการ์ดวางแผน OT จากผู้ใช้จริงบนมือถือและเดสก์ท็อป

## Open Issues / Notes
- **เรื่อง Cache บนมือถือ (PWA):** หากยังเห็นเวอร์ชันเดิม ให้กดรีเฟรช 1 ครั้ง Service Worker ตัวใหม่ `ot-v68` จะโหลดโค้ดเวอร์ชันล่าสุดทันที

